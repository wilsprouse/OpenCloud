# This Script installs the necessary software to host the bare minimum of OpenCloud: The User Interface.
# Consistent with OpenCloud's ideals, the subdependencies for each service are installed when a user enables the service on their cluster
#
#
#

# Install Go (Check if its installed already, if not, install it
# Install typescript and typescript dependencies for this repo
# Compile the Go app into a binary
# Compile the typescript into compiled TypeScript
# Move Go binary to systemd service
# Move compiled TypeScript into a service?
# Start the services
